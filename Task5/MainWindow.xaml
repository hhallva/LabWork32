<Window x:Class="Task5.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Task5"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <!--<Window.Resources>
        <DataTemplate x:Key="Template">
            <Image Source="plant.png" HorizontalAlignment="Left"/>
            <CheckBox x:Name="CheckBox" Content="Показать пароль"/>
        </DataTemplate>
    </Window.Resources>-->

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <ComboBox x:Name="ComboBox" Grid.Row="0" DisplayMemberPath="Login" SelectionChanged="ComboBox_SelectionChanged"/>
        <ListBox x:Name="ListBox" Grid.Row="1" SelectionMode="Multiple" DisplayMemberPath="Login" SelectionChanged="ListBox_SelectionChanged"/>
        <ListView x:Name="ListView" Grid.Row="2" DisplayMemberPath="Login" SelectionChanged="ListView_SelectionChanged"/>
    </Grid>
</Window>
    
    
